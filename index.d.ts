import { Country } from "./src/type/type";

export function countryToEmoji(arg: Country): string;
export const countryList: {
  emoji: string;
  name: string;
  code: string;
  code3: string;
  number: string;
}[]